"""
DriverDetection package initializer.

Ensures this directory is treated as a proper Python package so that
relative imports inside the modules (e.g., from .frame_processor import ...)
work reliably when imported dynamically.
"""

